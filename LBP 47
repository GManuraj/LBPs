#include <stdio.h>
int main()
{
    int i,j,count,sum=0,strt,end;
    scanf("%d", &strt);
    scanf("%d", &end);
    for(i=strt+1;i<=end;i++)
    {
        count=0;
        for(j=2;j<=i/2;j++)
        {
            if(i%j==0)
            {
                count++;
                break;
            }
        }
        if(count==0)
        {
            sum+=i;
        }
    }
    printf("%d",sum); 
}
