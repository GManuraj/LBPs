#include<stdio.h>
int main() {
    int n, i, j, flag = 0, x, temp;
    scanf("%d", &n);

    for (i = 1; i <= n; i++) {
        // Check if i is prime
        flag = 1; // Assume i is prime
        for (j = 2; j * j <= i; j++) {
            if (i % j == 0) {
                flag = 0; // i is not prime
                break;
            }
        }

        // If i is prime, check if it contains the digit 9
        if (flag == 1) {
            temp = i;
            while (temp > 0) {
                if (temp % 10 == 9) {  // Check last digit
                    printf("%d\n", i);  // Print prime number containing 9
                    break; // Exit after printing the first valid number
                }
                temp /= 10; // remove the last digit
            }
        }
    }
    if(flag==0)
    {
        printf("0");
    }

    return 0;
}
