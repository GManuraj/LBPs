#include <stdio.h>
int main()
{
	int n,x=0,y=1,z,sum=0;
	scanf("%d",&n);
	if(n==0)
	{
		printf("%d\t",n);
	}
	if(n==1)
	{
		printf("%d\t",n);
    }
	for(int i=1; i<=n; i++)
	{
		z=x+y;
		sum+=z;
		x=y;d
		y=z;
	}
	printf("%d",sum);
}
