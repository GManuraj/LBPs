#include <stdio.h>
int main()
{
    int i,j,count,strt,end;
    scanf("%d", &strt);
    scanf("%d", &end);
    for(i=strt;i<=end;i++)
    {
        count=0;
        for(j=1;j<=i;j++)
        {
            if(i%j==0)
            {
                count++;
            }
        }
        if(count==2)
        {
            printf("%d\t",i);
        }
    }
}
