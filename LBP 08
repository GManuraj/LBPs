#include<stdio.h>
int main()
{
    float l,b,p;
    scanf("%f %f",&l,&b);
    p=2*(l+b);
    if((int)l==l && (int)b==b)
    {
        printf("%.0f",p);
    }
    else
    {
        printf("%.1f",p);
    }
}
