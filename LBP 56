#include<stdio.h>
int main()
{
    int num,digit,largest;
    scanf("%d",&num);
    largest=0;
    while(num>0)
    {
        digit=num%10;
        if(digit>largest)
        {
            largest=digit;
        }
        num/=10;
    }
    printf("%d",largest);
}
