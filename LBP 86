#include<stdio.h>
int main()
{
    int num,digit,sum=0,temp;
    scanf("%d",&num);
    temp=num;
    while(temp!=0)
    {
        digit=temp%10;
        sum=sum+digit;
        temp=temp/10;
    }
    if(num%sum==0){
        printf("YES");
    }
    else{
        printf("NO");
    }
}
