#include<stdio.h>
int main()
{
    int res,n,digit,product=1,sum=0;
    scanf("%d",&n);
    while(n!=0){
        digit=n%10;
        sum=sum+digit*digit;
        product=product*digit;
        n=n/10;
    }
    res=sum-product;
    printf("%d",res);
}
