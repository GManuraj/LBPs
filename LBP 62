#include<stdio.h>
int main()
{
    int strt,end,i,r,temp,sum;
    scanf("%d %d",&strt,&end);
    for(i=strt;i<=end;i++)
    {
        temp=i;
        sum=0;
        while(temp>0)
        {
            r=temp%10;
            sum=sum+(r*r*r);
            temp/=10;
        }
        if(sum==i)
        {
            printf("%d\t",sum);
        }
    }
}
