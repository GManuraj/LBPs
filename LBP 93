#include<stdio.h>
int main()
{
    int n,rev=0,digit,temp,data;
    scanf("%d",&n);
    temp=n;
    while(temp!=0){
        digit=temp%10;
        rev=(rev*10)+digit;
        temp=temp/10;
    }
    data=rev+n;
    printf("%d",data);
}
